package view.view_components;

import javax.swing.*;
import java.awt.*;

public class Sidebar extends JPanel implements ViewComponent {
    public Sidebar() {
        setPreferredSize(new Dimension(230, 750));
        setBackground(claudeWhiteEmph);
    }
}
